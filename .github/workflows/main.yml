on:
  push:
    branches:
      - master

jobs:
  
  deploy:
    
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Deploy to server
        run: |
          whoami
          ls
          eval "$(ssh-agent -s)"
          ssh-add ./system/.ssh
          sudo chmod 755 ~/.ssh
          ssh friczky@34.101.143.10 << EOF
          cd /www/wwwroot/zakat.akademi.my.id/web-si-zakat-ci3
          git pull
          EOF
          
